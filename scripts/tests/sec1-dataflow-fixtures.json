[
  {"name":"DOM input through alias", "code":"const raw = document.getElementById('nick').value; const alias = raw; el.innerHTML = `\n${alias}\n`;", "states":["raw"]},
  {"name":"DOM reference and dataset", "code":"const input = document.querySelector('input'); const raw = input.dataset.nick; el.innerHTML = `\n${raw}\n`;", "states":["raw"]},
  {"name":"property assignment", "code":"obj.field = location.hash; el.innerHTML = `\n${obj.field}\n`;", "states":["raw"]},
  {"name":"storage source", "code":"const raw = localStorage.getItem('nick'); el.innerHTML = `\n${raw}\n`;", "states":["raw"]},
  {"name":"explicit esc alias", "code":"const safe = esc(input.value); const alias = safe; el.innerHTML = `\n${alias}\n`;", "states":["safe"]},
  {"name":"escapeHtml alias", "code":"const safe = escapeHtml(name); el.innerHTML = `\n${safe}\n`;", "states":["safe"]},
  {"name":"namespace helper", "code":"const safe = RoomkRTDB.esc(name); el.innerHTML = `\n${safe}\n`;", "states":["safe"]},
  {"name":"escaped HTML retained", "code":"const safe = esc(name); const html = `<b>${safe}</b>`; el.innerHTML = `\n${html}\n`;", "states":["html"]},
  {"name":"static HTML and later numeric context", "code":"const html = '<b>Hello</b>'; const count = 3; el.innerHTML = `\n${html} ${count}\n`;", "states":["html","unknown"]},
  {"name":"numeric text", "code":"const count = 3; el.innerHTML = `\n${count}\n`;", "states":["safe"]},
  {"name":"safe-looking name not trusted", "code":"const escapedHTML = location.search; el.innerHTML = `\n${escapedHTML}\n`;", "states":["raw"]},
  {"name":"reassignment", "code":"let safe = esc(name); safe = location.hash; el.innerHTML = `\n${safe}\n`;", "states":["raw"]},
  {"name":"append raw", "code":"let safe = esc(name); const raw = location.hash; safe += raw; el.innerHTML = `\n${safe}\n`;", "states":["raw"]},
  {"name":"future definition", "code":"el.innerHTML = `\n${safe}\n`; const safe = esc(name);", "states":["unknown"]},
  {"name":"function scope", "code":"function a() { const safe = esc(name); } function b(safe) { el.innerHTML = `\n${safe}\n`; }", "states":["unknown"]},
  {"name":"parameter shadow", "code":"const safe = esc(name); function render(safe) { el.innerHTML = `\n${safe}\n`; }", "states":["unknown"]},
  {"name":"branch merge", "code":"let safe = esc(name); if (flag) { safe = location.hash; } el.innerHTML = `\n${safe}\n`;", "states":["unknown"]},
  {"name":"unresolved function", "code":"const safe = makeHTML(name); el.innerHTML = `\n${safe}\n`;", "states":["unknown"]},
  {"name":"property alias mutation", "code":"obj.field = esc(name); const alias = obj; alias.field = location.hash; el.innerHTML = `\n${obj.field}\n`;", "states":["unknown"]},
  {"name":"base object replacement", "code":"obj.field = esc(name); obj = other; el.innerHTML = `\n${obj.field}\n`;", "states":["unknown"]},
  {"name":"unknown call invalidates", "code":"obj.field = esc(name); mutate(obj); el.innerHTML = `\n${obj.field}\n`;", "states":["unknown"]},
  {"name":"partial escape is not safe", "code":"const html = esc(name) + unknown; el.innerHTML = `\n${html}\n`;", "states":["unknown"]},
  {"name":"mixed escaped and raw template", "code":"const safe = esc(name); const raw = location.hash; const html = `<b>${safe}${raw}</b>`; el.innerHTML = `\n${html}\n`;", "states":["raw"]},
  {"name":"local helper redefinition", "code":"function esc(x) { return x; } const safe = esc(name); el.innerHTML = `\n${safe}\n`;", "states":["unknown"]},
  {"name":"similarly named helper", "code":"const safe = escapeWhatever(name); el.innerHTML = `\n${safe}\n`;", "states":["unknown"]},
  {"name":"attribute context not certified", "code":"const html = '<b>Hello</b>'; el.innerHTML = `\n<div title=\"${html}\">x</div>\n`;", "states":["unknown"]},
  {"name":"attribute greater-than stays quoted", "code":"const html = '<b>Hello</b>'; el.innerHTML = `\n<div title=\"> ${html}\">x</div>\n`;", "states":["unknown"]},
  {"name":"script context not certified", "code":"const safe = esc(name); el.innerHTML = `\n<script>const x='${safe}';</script>\n`;", "states":["unknown"]},
  {"name":"CSS context not certified", "code":"const safe = esc(name); el.innerHTML = `\n<style>${safe}</style>\n`;", "states":["unknown"]},
  {"name":"apparent nested markup inside script", "code":"const safe = esc(name); el.innerHTML = `\n<script><style></style>${safe}</script>\n`;", "states":["unknown"]},
  {"name":"JS escaped opening tag", "code":"const safe = esc(name); el.innerHTML = `\n\\x3cscript>${safe}</script>\n`;", "states":["unknown"]},
  {"name":"retained script HTML not certified", "code":"const safe = esc(name); const html = `<script>${safe}</script>`; el.innerHTML = `\n${html}\n`;", "states":["unknown"]},
  {"name":"source location shadow", "code":"const location = { hash: 'safe' }; const raw = location.hash; el.innerHTML = `\n${raw}\n`;", "states":["unknown"]},
  {"name":"source document parameter shadow", "code":"function f(document) { const raw = document.getElementById('x').value; el.innerHTML = `\n${raw}\n`; }", "states":["unknown"]},
  {"name":"source storage shadow", "code":"const localStorage = fake; const raw = localStorage.getItem('x'); el.innerHTML = `\n${raw}\n`;", "states":["unknown"]},
  {"name":"namespace helper replaced", "code":"RoomkRTDB.esc = fake; const safe = RoomkRTDB.esc(name); el.innerHTML = `\n${safe}\n`;", "states":["unknown"]},
  {"name":"single line remains legacy scope", "code":"const raw = location.hash; el.innerHTML = `${raw}`;", "states":[]},
  {"name":"comments and string fake sinks", "code":"// el.innerHTML = `\n// ${fake}\n// `;\nconst literal = 'el.innerHTML = `${fake}`'; /* el.innerHTML = `\n${fake}\n`; */", "states":[]},
  {"name":"regex fake sink", "code":"const pattern = /el.innerHTML = `\\n${fake}\\n`/;", "states":[]},
  {"name":"escaped template marker", "code":"el.innerHTML = `\n\\${notAnInterpolation}\n`;", "states":[]},
  {"name":"non-script HTML excluded", "html":"<p>el.innerHTML = `\n${fake}\n`;</p><script type=\"application/json\">{\"fake\":\"innerHTML\"}</script>", "states":[]},
  {"name":"inline script line offset", "html":"<html>\n<script>\nconst raw = location.hash; el.innerHTML = `\n${raw}\n`;\n</script>", "states":["raw"], "lines":[4]},
  {"name":"malformed input fails closed", "code":"el.innerHTML = `\n${unterminated", "failure":2}
]
