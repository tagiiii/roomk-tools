# A-11 手動レビュー集約と限定検査器の区切り（2026-09-09）

基準: `7625ba6ab1d77042bb52721bdd430fea92083fe7`。P-17へのオーナー回答は、検査器の高度化をいったん区切り、未判定44箇所を直接レビューし、必要な隔離検証を行うという条件付き承認である。未判定の放置・安全認定・全バックログ完了の承認ではない。

## 結論

**現行13アプリの対象44参照を全件直接レビューし、それぞれの表示経路で未エスケープの外部文字列がHTML構造へ入る経路は確認しなかった。** 全件について作成担当とは別コンテキストの担当も確認した。この範囲で新たな修正対象は特定されず、アプリと検査器は変更していない。

これは「機械が安全と証明した」でも「アプリ全体に脆弱性がない」でもない。入力元・変換・再代入・最終出力文脈を読んだ現行ソース限定のレビュー結果である。検査器は71ファイル・ERROR 0・裸参照44・**unknown 44のまま**。safeへの変更、無視リスト、既存証拠の書換えは行わない。

## 対応範囲と独立確認

| 担当報告 | 対象 | 参照数 | 独立確認 |
| --- | --- | --- | --- |
| [通信系](a11-manual-network-review-2026-09-09.md) | do-mannaka、jinro、kakure-number、name-change、tatoe-gp | 19 | ローカル系報告の作成担当がコード・表・SHAを再確認、追加修正要求なし |
| [混合系](a11-manual-mixed-review-2026-09-09.md) | kotoba-pair、kotoba-shuffle、kotoba-tantei、kyoumi-sugoroku | 14 | 通信系報告の作成担当がコード・表・SHAを再確認、追加修正要求なし |
| [ローカル系](a11-manual-local-review-2026-09-09.md) | quiz、suki-type-check、talk-card、value-card | 11 | 混合系報告の作成担当がコード・表・SHAを再確認、追加修正要求なし |

3報告の1参照1行の和集合を、現行scannerのfile・line・reference・unknown状態と機械照合した。44件一致、重複・欠落なし、13アプリ。共通ヘルパー・データ定義・serviceを含む重複除去後20ファイルのSHA-256も一致。主担当は各報告全文と外部数値・メモ・固定データ等の代表ソースを確認した。独立レビューは同系統の別コンテキストによるものであり、異系統モデルの二重承認とは記録しない。

## 検証と再実行

- `python3 scripts/tests/test-sec1-dataflow.py`: 158 fixtures、入力欠落失敗、shell統合9件、現行71ファイルの回帰がPASS。unknown 44を維持。
- 主担当が既存のSHA固定VM検証7本を再実行し、各stdoutをJSON化して保存JSONとdeepEqual一致を確認: `a11-jinro-night-2026-09-08`、`a11-jinro-tiebreak-2026-09-09`、`a11-kakure-display-2026-09-08`、`a11-value-memo-2026-09-08`、`a11-lobby-length-2026-09-08`、`a11-kotoba-player-list-2026-09-09`、`a11-kotoba-spec-board-2026-09-09`（全て `evidence/` の `.mjs` と `.json`）。ケースには対象の重複があり、全44件の実ブラウザ試験とは数えない。
- 各独立担当も、担当報告の人狼同票／ことば探偵3本／value-cardをそれぞれ再実行し保存JSON一致を確認した。固定quizパックとtalk-cardお題のメモリ内検査も独立に再確認した。
- [対応・SHA照合器](evidence/a11-manual-coverage-2026-09-09.mjs) は次で再実行できる。これはレビュー漏れとソース差替わりの検知であり、内容の正しさや安全性を自動判定しない。既存CIへの組込みは行っていない。
- 統合レビューで、照合器のSHA件数だけでは無関係なファイルへの差替えを見逃すと指摘され、対象13本＋依存7本の正確なパス集合照合を追加した。主担当のメモリ内対照では、行欠落・参照名交換・SHA不一致をすべて拒否した。独立担当の再確認でも、正常系PASSと依存差替え負例の拒否が成立し、最終統合レビューは追加修正要求なし。元ファイルを書き換えて負例を作ったわけではない。
- 最終機械チェック: lint全16項目、diff check、新規報告の相対リンク・索引対応がPASS。アプリ・検査器本体・既存fixture・既存VM証拠・禁止パスの差分はない。

```sh
node docs/reports/evidence/a11-manual-coverage-2026-09-09.mjs
```

VM検証は抽出関数とスタブによるHTML文字列・プロパティ捕捉であり、今回ブラウザDOM解析・実SDK・実画像保存・本番Firebase・iPhoneは実行していない。利用者向け挙動の変更はないため、新たな画面・同期試験を実施したとは扱わない。

## 継続課題と再レビューの条件

1. **自動解析の未解決44参照は継続管理する。** 関数間・map/join・入力元・型・文脈を検査器が一般的に証明できない状態は解消していない。手動レビュー済みという記録をsafe判定の根拠として検査器へ流し込まない。高度化の再開は別の作業範囲として扱う。
2. 対象ソース、import先のデータ／共有escape、値の生成元、再代入、挿入先を変える場合は該当行を再レビューする。SHA不一致時は古い報告の適用を止め、差分と影響を追跡する。ハッシュだけを更新して再承認しない。行番号が変わっただけでも対応を再照合する。
3. 特にquizのicon、talk-cardのお題、kotoba-shuffle／kyoumi-sugorokuの固定ラベル等は、現在固定値であることに依存する。自由入力・Firebase・URL・ファイル取込みへ外部化するときは、そのままHTMLへ入れず出力文脈に合う防御と対照検証を追加する。
4. HTML属性のescapeは、CSS値・役割enum・数値の意味的正しさ・認可を保証しない。suki-type-checkの幅は数値計算という前提も必要。ことば探偵の不正なplayers型による描画停止、数値のNaN/Infinity等は各報告に限界として残し、正常動作・適切な入力検証だとは扱わない。この工程では修正していない。
5. 任意JS実行による組込み改変、依存ライブラリ侵害、他のsink、全通信・DB権限・情報秘匿までの保証はない。今回の結果だけでこれらのリスクを受容済み・解消済みとしない。保留6件、C-1等の未完了項目、性格に基づく推薦をしない方針は変更しない。

A-11はP-17で変更された範囲、すなわち**限定検査器の実装・保証範囲明文化・現行44参照の手動レビューと独立確認**について区切る。一般解析能力の完成や未判定の自動解決とは別である。旧報告の未完了表記は当時の履歴として保存する。コミット・PR・公開の結果はGit履歴とPR／Actionsを参照し、未実施の工程を本報告から推測しない。
