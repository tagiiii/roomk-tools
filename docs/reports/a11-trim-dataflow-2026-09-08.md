# A-11: trim後の裸補間の限定追跡（2026-09-08）

## 変更と結果

`scripts/sec1-dataflow.py` に、既知のraw入力・裸参照へ引数なしの `.trim()` を適用した式全体をrawのまま追跡する限定処理を追加した。空白除去はHTMLエスケープではない。対象は既存のDOM値・DOM別名・location・storageとその単純別名で、safe/htmlを新たに認定する処理は追加していない。アプリ・共有モジュール・rules・lint本体・CIは無変更。

恒久fixtureは既存45件を保持して31件追加、計76件PASS。追加31件の同一入力を変更前の検査器とも照合し、8件はunknownからraw（ERROR）になり、23件は従来判定を保持した。入力欠落時の失敗と実際のSEC-1 shell統合9ケースもPASS。現行71ファイルの裸参照44件は全てunknown、ERROR 0のまま。着手前後のlint全16項目・diff checkは通過した。

- 検出例: DOM取得値・DOM別名・入力別名・location・window.location・local/sessionStorage・コメントを挟む通常呼出し。
- 対照: 既存escapeHtmlで外側を包む入力源別の対例、エスケープ済みreceiver、未知の独自メソッド、引数付き、変換連鎖、連結、optional/computed呼出し、括弧付きreceiver、同名関数・組込み置換の手掛かり、入力元の影・再代入、文字列や類似メソッド名。
- 既存メモ組立の正常／escape欠落2件は両方unknownを維持した。今回の拡張だけで実際のvalue-cardの配列・callback・条件式を追跡できるようにはなっていない。

## 境界

末尾4トークンが厳密に `. trim ( )` のときだけ、receiverを裸参照または既存の単純なDOM/storage入力式に限定して評価する。連結全体をreceiverとして解釈しない。既存の副作用候補・ブロック境界による根拠失効は維持した。

同一script内の明示的なtrim宣言・文字列キーやprototype／defineProperty等の置換の手掛かりは、この追加追跡をscript全体で無効化する。無関係な同名宣言も除外され得る保守的な方法であり、動的計算キー・別script・外部コードによる組込み改変を完全に検出する仕組みではない。対象コードを実行せず、組込みメソッドの実体や本番での悪用可能性を証明しない。

`raw`は監査すべき入力経路を表す検査エラーであり、脆弱性の実害確定ではない。未判定44件を安全認定せず、A-11全体は未完了。保留7件・本番Firebase・iPhone操作は対象外。変更は未コミット・未公開。

## 独立レビュー

生成者とは別コンテキストの同系統エージェントが、76fixturesとshell統合9ケース、追加31件の旧検査器比較（8件だけunknown→raw）、別途11境界ケース、報告・README・バックログを確認して承認した。主担当も修正前後のlocation.hash.trim()の分類変更、前工程45件の完全保持、76件PASS・lint・対象外差分なしを確認した。追加の境界確認11件は恒久fixtures76件とは別の読み取り専用レビューであり、件数に加算していない。
