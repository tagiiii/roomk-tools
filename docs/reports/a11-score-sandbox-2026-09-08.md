# A-11 点数表示4箇所の隔離検証

2026-09-08、オーナーの「お願いします」により[未判定棚卸し](a11-unknown-triage-2026-09-08.md)の優先4箇所を検証。基準HEADは `8714c57927a01e50d5b1130d865ad796484b9ada`。アプリ修正ではなく固定入力による検証のみ。

## 結果

do-mannakaの1526/1558行、tatoe-gpの1160/1877行のscore表示は、文字列を数値へ変換せずHTMLエスケープもしないままinnerHTMLへ挿入する。無害な太字タグをscoreに渡すと4箇所ともタグを保持した文字列を生成し、その捕捉出力をローカルブラウザで解析すると実際のB要素になることを確認した。

これは**異常なscore値が表示関数へ届いた場合のHTML混入再現**。本番DBへの書き込み権限、保存セッションから実DBへの到達、他ユーザーへの攻撃可否、JavaScript実行は未検証。4件の本番XSSを実証したという意味ではない。

## 方法と証拠

- [固定検証スクリプト](evidence/a11-score-sandbox-2026-09-08.mjs)は、2アプリと共有escapeHtmlの基準SHA・全バイト一致を検査し、正確な開始/終了行と関数境界をassertして4関数を抽出する。汎用パーサではない。
- 元の関数をVM上で実行し、documentをinnerHTML/textContent捕捉用stubへ置換。SDK、ネットワーク、storageは与えず、入力はコードとして実行しない。
- 12、文字列12、null、0、`<b data-a11-score-probe="1">probe</b>` の5値×4箇所、空players×4、HTMLを含む名前×4の計28ケースがPASS。これは「意図した観察を再現した」の意味で、脆弱性修正テストの合格ではない。
- [結果JSON](evidence/a11-score-sandbox-2026-09-08.json)は入力別結果・source/関数SHA・完全な捕捉HTML4件を含み、再実行出力との一致を確認した。
- 名前は既存escapeHtmlでエスケープされていた。最終勝者名・点数のtextContent代入はraw文字列を保持してもHTMLとして扱う経路ではなく、ランキングinnerHTMLとは区別した。空playersは新しいstubでの確認だけで、前画面からの残存表示は対象外。

## ブラウザでの追加確認

主担当が上記JSONのcapturesから一時HTMLを作成し、localhost:8782のみで開いた。ページはscriptを含まず、CSP `default-src 'none'; base-uri 'none'; form-action 'none'` を設定。マーカーにはイベントハンドラや外部URLを含めていない。Browserスキルで表示DOMを読み、各sectionに `b[data-a11-score-probe="1"]` が1個、tagNameはB、本文はprobeであることを確認した（4/4）。console errorログは空。

これは実アプリのUI・Firebase SDKを起動した試験ではなく、実関数が生成した捕捉HTMLのブラウザ解析試験。画面遷移、端末幅、複数ユーザー同期、スクリプト実行までは検証していない。本番Firebase・iPhone・OSクリップボードを操作していない。

## 判断へ渡す最小修正案

P-16として、両アプリの該当4箇所だけを `${score}` から `${escapeHtml(score)}` へ変更する案を起票する。既存共有関数はString化後にHTMLの特殊文字を置換するため、数値表示を保ちながら異常文字列のタグ解釈を防ぐ。共有関数自体は変更しない。1アプリ1コミットとし、修正後はマーカーが要素にならないこと・通常点数・名前・勝者表示を再検証する。

数値検証・順位計算・データ書き込み・rules変更はこの案の対象外。不正な値による順位の乱れまで直したとは扱わない。承認前にアプリを変更しない。A-11の検査器は今回無変更で、未判定48件の解消とも扱わない。

## 検証・片付け

同系統の別コンテキストによる独立レビューで、固定ソースと抽出範囲、共有escapeHtml実体、28ケースのassertion、再生成JSONと保存JSONの完全一致を確認し承認された。ブラウザの構造確認は主担当によるもの。lint全16項目・diff check通過、apps/scriptsの差分なし。先行の未判定棚卸し文書は保持した。

テスト用タブ・サーバーを終了し、一時HTMLと空スクラッチディレクトリを削除した。恒久JSONのcapturesから同じ表示入力を再構成できる。今回の報告・検証器・起票は未コミット・未公開。

後続工程（2026-09-08）: P-16承認後の修正は[修正記録](p16-score-escape-2026-09-08.md)を参照。本報告は修正前の履歴であり、旧検証の再現は `node docs/reports/evidence/a11-score-sandbox-2026-09-08.mjs --baseline` で固定Gitソースを使用する。引数なしでは修正後の作業ソースと基準の不一致を検知して停止する。
