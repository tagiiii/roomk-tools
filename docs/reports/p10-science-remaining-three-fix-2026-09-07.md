# P-10 残る科学3問の承認修正・検証記録（2026-09-07）

## 承認範囲と固定基準

オーナーが [残る科学3問の提案書](p10-science-remaining-three-proposal-2026-09-07.md) の z08／ks08／ks13 の全文案を承認したため、3問の `question`／`choices`／`explanation`、計9フィールドだけを変更した。文案・出典の支持範囲・正答一意性・体験上の変更点は同提案書を参照する。提案書の「未承認・未実装」は当時の履歴として保持し、他の残余問やP-10／C3の運用判断への承認には広げない。

| 固定対象 | 値 |
|---|---|
| 公開済みmain | `c5577f92cab134fc71a1e6b03d4513d717c6aefb`（PR #16 マージ後） |
| 修正前questions.js SHA-256 | `e11de150d284b666c98e53369c102ecb3069816af3a60569ef46c2512b6032d3` |
| 修正後questions.js SHA-256 | `453880d703e04c36dca8f8d37f88727813cc58e0ac9f9586368cd84f1fd2f4db` |
| 旧提案書SHA-256 | `0efa381990df1668c24bfecdf81eafcc0070b65cd93dfe68c8ed365c957d68c6`（変更なし） |

z08は鳥の骨内部に空気の入る空間が存在することを問う形へ変更。ks08は温度変化の速さを問わず、直径を指定した丸い水面の広さを直接比較する形へ変更。ks13は晴天・曇天の湿度を比較せず、日差しによる加熱と蒸発の働きを問う形へ変更した。未裏付けの比較・一般化を出題から外す対応であり、旧主張のすべてが誤りだったと証明するものではない。

他477問、全パックのメタデータ、ID・difficulty・配列位置・正答番号は不変。正答はz08が②、ks08／ks13が①。ks08は数値と「直径」が加わり、温度についての推理から面積の直接比較へ変わることも承認範囲に含む。

## 機械検証・独立レビュー（担当結果受領）

新しい [固定スナップショット検証器](evidence/p10-science-remaining-three-fix-2026-09-07.mjs) はPASS。24パック・480問、指定9フィールドの厳密な文字列置換、他477問・全メタデータの保持、旧証拠・旧検証器29ファイルと旧提案書の不変を確認した。旧証拠や過去の固定SHA検証器は更新していない。

変更のないapp.jsの実コピー関数を用い、転送先だけをスタブ化して3問の問題・正答解説の計6文字列を全文照合した。OSクリップボードへは転送していない。従来のtextContentによる設問・解説表示と選択肢のエスケープ経路も不変で、新しい実行コードやHTML挿入経路は追加していない。

同系統の別コンテキストによるsource・検証器の独立レビューもPASS。実ファイルを変更しない負例4件を検出した。この確認は差分・構造・検証器の検出範囲に関するもので、全事実や全意味重複の保証ではなく、異系統の二重レビューとは称さない。

画面確認後の本報告書への追加レビューは担当の利用制限で未完了となったため、主担当が実測結果と記録を最終照合した。完了済みのsource・検証器レビューとは区別し、本報告書の独立レビュー完了は主張しない。

lint全16項目PASS、content-auditは既知coverage警告7件を継続。diff checkはexit 0。実装の検証結果を記録するもので、OSクリップボードへの実転送とは区別する。

## 主担当による画面確認（結果受領）

ブラウザ担当の接続先が `Browser is not available: 1` となったため、主担当の既存接続で検証を引き継いだ。これは接続制約であり、アプリの不具合とは扱わない。接続できなかった担当自身が画面検証を実施したとは記録しない。実appsを新しいローカルorigin `http://127.0.0.1:8776/quiz/` で配信し、標準のlocalhost時のstats無効化を利用した。本番Firebaseには接続していない。

- PC 1280×720: `zatsugaku-3choice`／`kurashi-suiri` の各20問を全正答で完走し、それぞれ20/20。
- 375×812: 同じ2パック各20問を全誤答で完走し、それぞれ0/20。
- 各実行で重複しない20 IDを確認。全問のDOM解説全文を期待ソースと照合し、選択後の全ボタン無効化と横はみ出しなしを確認した。
- 対象z08／ks08／ks13のPC正答と375px誤答を確認。さらに375pxで雑学を再プレイし、z08の①を誤答として選び、11問回答時の途中終了10/11を確認。TOPからくらしを選び直した追加実行ではks08／ks13の③を誤答として選び、13問回答時の途中終了11/13を確認した。完走経路と合わせ、対象3問の全3選択肢を検証した。
- 終了・再プレイ・途中終了と、最終TOP復帰を確認した。

対象3問×2幅の通常viewportで計6枚のスクリーンショットを目視した。ks08の長文は適切に折り返され、横スクロールはない。縦スクロールは必要で、固定FABが右下にあり、撮影時に解説下部の一部へ重なっている。文字レイアウト自体の横はみ出しはないが、画像だけで全解説が遮られず同時に見えるとは扱わない。

専用error／warnログをPC完了後・375px完了後・最後などに取得し、いずれも空配列だった。この試験経路と取得範囲の結果であり、全通信・全利用環境の保証ではない。初回の選択子表記違いによるクリックタイムアウト2回と、操作用JavaScriptの構文エラー1回は検証操作側で訂正した。これらに起因する画面挙動の不具合は確認されていない。

後片付けではタブ9を閉じ、タブ一覧が空であることを確認。viewportのreset呼び出しは成功したが、既定寸法への復元は再測定していない。自ら起動したPythonサーバーPID 87909のcommandを照合後、SIGINTで停止し、8776のlistenerがないことを確認した。iPhone実機操作とOSクリップボードへの実転送は未実施で、コピー確認は前記の文字列検証のみ。

## 残余と公開状態

承認された3問の修正・検証を終え、旧残余9問からz08／ks08／ks13を対応済みとして除外した。残る6問は kbn07、kj19、ru18、zk201、zk203、zk211。この6は誤り数でもバックログ全体の未完了数でもない。P-10／C3の運用判断は保留のまま。

今回の修正は未コミット・未push・実PR未作成・未公開。公開済みmainとは区別する。検証中のコード追加変更はなく、既存の文書差分・旧報告・旧提案書は保持した。コミット・push・PR・デプロイは行っていない。
