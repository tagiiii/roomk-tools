# A-11 バリューカード画像保存メモの隔離検証（2026-09-08）

## 対象と方法

基準コミット `09086e20c5d8090085829a2e2a2d233c1e9ffd06` の `apps/value-card/app.js:304`（`cardsHtml`）を対象に、`saveImage()` 全体（243〜329行）と共有 `escapeHtml()` 実体（`apps/shared/js/utils.js:168`〜175）をハッシュ固定で抽出した。ソースは作業ツリーと基準コミットの一致を確認する。

固定カードと固定 textarea 値だけを Node VM に渡し、一時 DOM・300ms タイマー・html2canvas・canvas・リンククリックをスタブ化した。SDK・本番 Firebase・ネットワーク・実利用データ・ブラウザ・実ダウンロードは使用していない。

- 再実行: `node docs/reports/evidence/a11-value-memo-2026-09-08.mjs`（将来の作業ツリー変更後は `--baseline` で固定コミットだけを読む）
- [検証器](evidence/a11-value-memo-2026-09-08.mjs) / [固定結果 JSON](evidence/a11-value-memo-2026-09-08.json)

## 結果

12ケースが期待した挙動に一致した。PASS は各 assertion との一致であり、同期例外ケースまで正常処理されるという意味ではない。

| ケース | 観察・assertion |
|---|---|
| 通常メモ、空、空白のみ | 前後空白を除去。空メモのブロックは出力しない |
| 無害な HTML マーカー | 生の `<b>` タグなし。`&lt;b…&gt;` としてエスケープ済みの文字列を確認 |
| 引用符・アポストロフィ・`&`・`<`・`>` | 期待した文字参照に一致 |
| 改行、100文字 | 生成 HTML 文字列に保持。画像上の改行表示・入力欄の maxlength 制約は未検証 |
| 3枚のメモ順（中間は空） | 各カードとの対応・順番を保持 |
| カードの名前・説明に特殊文字 | 両方のエスケープを確認（実カードデータの変更なし） |
| 成功時 | スタブクリック1回・PNG指定とファイル名一致・一時コンテナ削除 |
| html2canvas の Promise reject / toDataURL 例外 | スタブクリックなし。既存 catch が無言で処理し finally で一時コンテナ削除 |
| html2canvas の同期 throw | Promise チェーン成立前なので例外が伝播し、一時コンテナは残る。スタブで条件を強制した既存境界の観察で、実ライブラリでの発生条件は未確認 |

負の対照として VM に渡すソースコピーの `escapeHtml(memo)` 一箇所だけを `memo` に置換した。同じ HTML メモ assertion が失敗することを検証器自身が確認した。アプリファイルは変更していない。

## 判断と限界

対象の自由入力は `trim()` → `escapeHtml(memo)` → `cardsHtml` → `container.innerHTML` と流れ、今回の固定入力では未エスケープのメモ混入を再現しなかった。これは文字列出力の隔離検証であり、ブラウザによる DOM 解析・画像品質・実機 UI・実際のダウンロードの保証ではない。同期例外時の後片付け範囲は上表に限定して記録し、本件では修正しない。

scanner 自体や fixture は変更していないため、A-11 の機械分類は未判定44箇所のまま。対象外の経路の安全性へ一般化しない。アプリ・共有・scripts・rules は無変更。本検証器・証拠・報告書は未コミット・未公開で保存した。

## 検証と独立レビュー

生成者とは別コンテキストの同系統エージェントが、固定ソース・抽出境界・12ケース・負の対照・失敗経路の区別・報告書を確認し承認した。通常実行と `--baseline` の結果は保存JSONと完全一致し、主担当も再確認した。lint全16項目・diff check、および既存検査器43fixtures・入力欠落・shell統合8ケースはPASS。前回のことば探偵の未コミット記録は保持し、保留7件は変更していない。
