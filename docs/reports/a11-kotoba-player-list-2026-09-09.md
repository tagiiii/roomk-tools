# A-11 ことば探偵の参加者一覧・隔離検証

日付: 2026-09-09。基準コミット: `2709ee6296e4b8b1dfb33305f51db5409d616563`。

## 対象と結論

`apps/kotoba-tantei/app.js` の通常ロビー（306行）・観戦ロビー（1544行）の `playerList` 裸参照2箇所に関係するHTML文字列生成を検証した。正常系30群とエスケープ欠落対照4群、計34群が通過した。検証範囲で修正を要する不具合は確認していない。

これは限定入力・限定経路の証拠であり、検査器のsafe判定へ再分類しない。A-11は未完了、静的未判定44箇所と判断保留6件は維持する。アプリ・検査器・共有ファイル・rulesは変更していない。

## 方法と証拠

- [再実行ハーネス](evidence/a11-kotoba-player-list-2026-09-09.mjs)
- [保存結果JSON](evidence/a11-kotoba-player-list-2026-09-09.json)

リポジトリルートで実行する。

```sh
node docs/reports/evidence/a11-kotoba-player-list-2026-09-09.mjs
```

実ソースから `renderLobby`・`renderLobbyPlayer`・`renderPlayerControls`・`renderSpecLobby`・ラベル変換関数と共有 `escapeHtml` を抽出し、Node VMで実行する。ソース全体のSHA256を固定しており、変更後は照合エラーになる。その際は期待値だけ更新せず、抽出箇所とテストの再レビューが必要。

| ソース | SHA256 |
|---|---|
| apps/kotoba-tantei/app.js | b96fc56f8c3836b06a9ca6be6c58db65d812f9e870f9e45102b3f6924c0fcae9 |
| apps/shared/js/utils.js | 533e791fd06c37839989a1461bb0322d11c94fc1d1ef853a03c68fb12c25c370 |

購読、開始可否、開始条件表示、共有欄、観戦ヘッダーは代替処理。通常ロビーには閲覧者を合成追加する。参加UIの文字数制約を超える攻撃文字列も描画関数へ直接渡すため、通常参加操作の再現ではない。

## 確認内容

7種類の固定入力を、編集権限あり／なし・loadingあり／なしの4組合せで検証した（28群）。通常文字、引用符・アンパサンド・山括弧、HTMLを含む名前とID、ホスト、名前だけの攻撃値、IDだけの攻撃値、異常なteam／role文字列を含む。混在一覧と空の観戦一覧を加え、正常系は30群。

- 名前は文字列中でエスケープされ、生のimg／scriptタグは現れない。
- 編集用ID属性はエスケープされ、捕捉した属性文字列を復号すると元のIDと一致する。DOMによる属性解析ではない。
- ホストの行に削除ボタンはなく、ゲストの行には編集権限がある場合だけ表示される。非ホスト閲覧・観戦一覧には操作ボタンがない。
- loading時の編集ボタンはdisabled。team／roleは固定ラベルへ変換され、操作属性も固定候補に限られる。
- 名前表示2経路、削除用ID属性、割当用ID属性の計4箇所で、それぞれエスケープをメモリ内だけで除去した。同じ検査がassertion失敗を検出することを確認した。アプリ本体への変異はない。

## 検証限界とレビュー

HTML文字列の検証であり、ブラウザのDOM解析・イベント・スクリプト非実行、Firestore購読・認可、観戦機能全体の書き込み禁止、セッション、ゲーム画面、参加人数の受入条件、型不正データの包括的処理は保証しない。本番Firebase・SDK・ネットワーク・実端末には接続していない。参加人数は別の[既存検証](a11-lobby-length-2026-09-08.md)を参照する。

作成担当とは別コンテキストのレビュアーが実ソースとハーネスを照合し、34群の再実行と保存JSON一致を確認して承認した。同一モデルのコンテキスト分離であり、異なるモデル系列による二重レビューではない。

既存検査器の回帰114件・shell連携9件は通過し、現行71ファイルの未判定44・エラー0を維持。lint・diff checkも通過。この工程は証拠と文書の保存までであり、コミット・push・PR・デプロイは未実施。
