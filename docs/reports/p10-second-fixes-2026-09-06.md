# P-10 次の5件の修正・検証記録（2026-09-06）

## 承認範囲と結果

オーナーが[残余候補の再照合と修正案](p10-next-candidates-2026-09-06.md)の優先5件に「はい」と回答したため、`ru10`／`khm05`／`s03`／`do16`／`zk210` のみを承認案どおり修正した。問題データの追加差分は9行置換。**未コミット・未公開**であり、push・PR・デプロイ・本番 Firebase 接続・iPhone 操作は行っていない。

- HEAD: `3a8b174d07b0e5302aeaf406e75f16fd38d7966e`
- 修正前 `questions.js` SHA-256: `79462941efcdde0e0de986162dc5d13d0dd53c1e796324e4e329cc95010e36e8`
- 修正後 SHA-256: `058162a845442933af2e95caf7442dda5b5a3f4b3d66821a1107db57472b19ae`
- [前回の5問修正](p10-priority-fixes-2026-09-06.md)と既存の未コミット文書・証拠を保持。今回は他475問を変更していない。
- [変更前後の問題データ・検証証拠](evidence/p10-second-fixes-2026-09-06.json)

## 変更内容

| ID | 変更した部分 | 修正の要点 |
|---|---|---|
| ru10 | 問題・選択肢・解説 | 「耳にする」の意味を尋ねる単一正答の問題へ。「聞きつける」を能動的探索だけとしない |
| khm05 | 問題・解説 | 「珈琲」「倶楽部」の音を当てる例に限定し、広義の当て字には熟字訓も含まれることを説明 |
| s03 | 解説のみ | 円網の粘着糸と非粘着糸を説明し、クモが歩く場所についての過剰な断定を削除 |
| do16 | 解説冒頭1文のみ | 勝利・成功などの結果を得る意味に限定。後続文は維持 |
| zk210 | 問題・解説 | 携帯電話での普及と1999年の176種類公開を区別し、絶対的な起源年や「めずらしい例」を断定しない |

根拠の出典・取得範囲・受入条件は提案報告が正本。`kbn08` の移動方法は未承認で未修正。`kbn10` の誤字指摘は撤回済みで、問題データは変更しない。問題追加・削除・難易度調整・他パックへの展開はしていない。

## 差分と内容の検証

実装は別コンテキストへ委譲し、主担当が作業前ファイルとの全480問比較を別途実行した。24パック・480問の在庫、パックメタデータ、問題ID、難易度、問題順、選択肢数、正答番号を維持。変更は指定5問だけで、先行5問を含む他475問は完全一致した。HEAD 比較では先行分と合わせて10問・24行置換となる。

実装者とは別コンテキストの読み取り専用レビューで承認、追加修正要求なし。小学館辞典・百科事典、当て字の辞典本文、Australian Museum、文化庁資料の項目033、MoMA の関連本文を再確認した。同系統の別コンテキストであり、異系統モデルによる二重承認ではない。レビュー対象は上記修正後SHAと一致。

セキュリティ面ではデータ文字列のみの変更で、HTMLやスクリプト、イベント配線、通信処理は追加していない。実装の問題・解説表示は `textContent`、選択肢は既存の `escapeHtml` 経路を維持。lint のXSSチェックを含む全16項目が通過した。新たな攻撃入力の実ブラウザ試験や全アプリのセキュリティ再監査ではない。

## ローカル画面検証

`127.0.0.1:8765` に今回専用のサーバーを起動し、実際の quiz を通常UIから操作した。`stats.js` だけはサーバーのメモリ上で無処理の応答に置換し、Firebase SDK を読み込まない。アプリ本体・問題データ・共通ユーティリティは実ファイルを配信し、状態の直接書換えや問題データのスタブ化は行っていない。

- 「ぜんぶ」モードで各対象問題まで進め、5件すべての正答判定・正答表示・解説が修正データに一致することを確認。回答後の選択肢は無効化された。
- 5件とも1280px／375pxで文書幅が画面幅を超えず、選択肢ボタン幅は760px／347px。画面幅変更後の安定した通常キャプチャで5件の375px表示とs03のPC表示を確認。zk210はスクロールして解説末尾と終了ボタンまで確認した。
- `ru11` で意図的に誤答し、「おしい！」と正答が表示された。類義語パックを最後まで進め、結果 `19 / 20` を確認。
- ことばの雑学王2は全20問の正答操作で `20 / 20` を確認。ここでの「正答」はアプリの正答番号に従う操作で、未修正問題の事実内容を保証するものではない。
- 途中終了で連続2択 `6 / 6`、同音異義語 `13 / 13`、漢字のひみつ `10 / 10` を確認。未回答分が分母に含まれない。TOPへの復帰も確認。
- タブのコンソールエラー0件。

注意：画面幅変更の直後とfullPageキャプチャで縮小・切り取り状の描画が生じたため、それらはレイアウト判定に使用していない。安定後の通常キャプチャとDOM寸法で確認した。最初の選択肢クリックは表示名の空白差、パック選択は子要素を含む表示名との差でテスト側のセレクタが一致せず、DOMを確認して修正した。アプリの修正は不要だった。

## コピー・機械チェック・後処理

- 実 `app.js` の `formatQuestionText`／`copyQuestion`／`copyAnswer` を抽出し、コピー先だけスタブ化したNode VMで5件を検証。問題・丸数字付き選択肢・正答・解説が修正データに一致。**実クリップボードへの転送は今回未検証**。
- `git diff --check`、`bash scripts/lint.sh`、`node scripts/content-audit.mjs` は通過。content-auditは既知coverage警告7件を維持、アプリ内完全重複0件。機械チェックを事実内容の保証とはみなさない。
- テスト用タブを閉じ、画面幅指定を解除。今回起動したサーバー（PID 89726）を停止し、8765ポートの待受なしを確認。テスト用コピーや設定ファイルは作成していない。
- `apps/updates.json` は未変更。既存問題の誤り訂正で、問題追加・機能追加・遊び方変更ではないため軽微bugfixとして扱う。

初回の具体的矛盾候補12件については、これで10件をローカル修正、1件は監査指摘撤回、残る修正候補は `kbn08`。ただし注意・出典不足・定期運用判断は別に残るため、P-10／C3 は閉じない。初回台帳は監査時点の履歴として保持し、現在の修正状況は後続報告で読む。コミット・公開と残る候補の修正には別途承認を得る。
