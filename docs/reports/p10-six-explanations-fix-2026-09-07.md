# P-10 承認済み6問の解説修正（2026-09-07）

## 変更と承認範囲

オーナーが[第3回追加照合](p10-followup3-2026-09-07.md)で提示した6問の最小案を承認し、`apps/quiz/questions.js` の対象6件の `explanation` だけを変更した。ky20は第2文を語義の対照へ置き換え、他5問は第2文を削除した。問題文・選択肢・正答番号・ID・難易度・順序・パック情報、および他474問は不変。

| ID | 修正後の解説（全文） |
|---|---|
| ky01 | 「口が重い」は、口数が少なくなかなか話さない様子。 |
| ky04 | 猫の手でも借りたいほど忙しい、という意味。 |
| ky18 | 「手を焼く」は、手に負えずに苦労すること。 |
| ky20 | 「口がかたい」は秘密を守れるということ。「口が重い」は、口数が少なくなかなか話さないこと。 |
| sk03 | 「この」の直後に「ノート」と続いているので、指しているのは「ノート」。 |
| sk05 | 「耳が痛い」は自分の失敗や弱点を指摘されて、聞くのがつらいという意味。 |

今回の承認を他の表現案、出典不足の許容、P-10／監査カタログC3の運用判断へは拡張しない。コミット・push・PR作成・デプロイは未実施で、修正は未公開。本番Firebase接続・iPhone操作も行っていない。先行12問の修正はPR #13・#14で公開済みだが、本6問の修正とは別工程である。

- 比較基準: `6afcb707378e4bcc3121b778d1102e51e36b9f5d`（main、PR #14マージ後）。
- 修正前SHA-256: `18d6abfe00f2348a1c182342c11992ec4a77d4d8a3fb40376724d84dc2fe5dc1`。
- 修正後SHA-256: `b1c59407e8d22b0a99cfe178bb1e24122e67a9a28172ad253376def1638f3c42`。

## 残余主張の追跡

第3回追加照合の未解消19問のうち、上記6問は未支持の文を削除・限定する承認済み対応により追跡対象から除き、残りは13問となった。**新たな出典によって旧主張が正しいと証明されたのではない**。過去台帳の `unresolved` や旧原文を書き換えず、この記録で修正後の扱いを示す。

- 科学・暮らし6問: z07、z08、s06、s08、ks08、ks13。
- 漢字・文化6問: kj19、kbn07、zk201、zk203、zk204、zk211。
- 語義1問: ru18。

13問は誤り数でも全バックログの未完了数でもない。残る表現案12問と出典待ちzk211は未承認のまま、P-10／C3の検証運用（範囲・頻度・未検証の許容）も未決定のため閉じない。

## 検証と限界

[今回の検証器](evidence/p10-six-explanations-fix-2026-09-07.mjs)を主担当が実行し、PASSを報告した。記録担当も現行6解説と修正後SHAの一致を読み取り専用で確認した。

検証器は24パック480問、承認した6解説だけの文字列置換、他474問・全メタデータの不変、承認案との一致、既存のP-10証拠JSON・検証器26ファイルのハッシュ保持、19→13の追跡対応を確認する。変更していない `app.js` の実際の問題・答えコピー関数を実行し、コピー先だけをスタブ化して出力文字列を照合する。**OSのクリップボード転送は未検証**であり、文字列検証を実機転送の検証とは扱わない。

第3回追加照合報告と旧証跡は履歴として保持する。旧検証器は修正前SHAに固定されているため、現行問題に対してSHA不一致になるのは想定どおり。旧検証器を書き換えて通さず、今回の新検証器を用いる。

### ブラウザ確認（主担当からの報告）

| 項目 | 記録時点の結果 |
|---|---|
| PC 1280px | 慣用句20問・総集編10問を全問正答で完走。対象6問の新解説、回答後の3ボタン無効化、横方向のはみ出しなし、20問・10問の結果とTOP復帰を確認 |
| 375px・誤答経路 | 慣用句20問・総集編10問を全問誤答で完走（0/20・0/10）。対象6問の解説全文一致・3ボタン無効化・横はみ出しなし・TOP復帰を確認。追加で再プレイ→1問正答→途中終了1/1→TOP復帰も確認 |
| 別コンテキストの独立レビュー | 修正要求なし。承認全文・6項目のみの差分・旧証跡保持・実コピー関数テストを独立確認。同系統の別コンテキストであり、異系統の二重レビューとは称さない |
| 最終機械チェック・差分確認・テスト後片付け | 主担当の検証器・lint全16項目・content-auditはいずれもexit 0。content-auditの既知coverage警告7件は継続。ローカルサーバー2本を停止、表示幅を元に戻した。テスト用アプリコピーや設定ファイルの作成なし |

初期のブラウザ確認では旧キャッシュを検出したため、新しいローカルorigin（ポート8766）へ切り替えて再試験した。初期の選択肢locatorも空白の不一致を修正して再開した。これらはテスト手順上の問題として記録し、アプリの不具合や旧表示での合格とは扱わない。

375pxのky20で全ページ撮影画像だけが極端な細幅になったため、その画像を正常表示の証拠には使わなかった。通常撮影でky20・sk03を再確認し、DOMのmain幅375px・問題文幅311px・横はみ出しなしと正常な表示を確認した。撮影時に限定された現象と判断しアプリのCSSは変更していないが、全ページ撮影機能自体の正常動作は保証しない。PCでもky20・sk03を画像確認した。画像は会話内で確認し、リポジトリには保存していない。

セキュリティ確認では新たなコードやHTML挿入の追加がなく、解説表示は従来どおり `textContent`。consoleログの専用取得とOSクリップボード転送は未実施で、エラー0や転送成功を主張しない。content-auditの集計は entries 3405／exact 255／intraAppExact 0／crossVariant 2／structural 12／similar 268で前回と同じ。

最終記録・README・P-10追加行も別コンテキストで確認し、重大な不整合なし。ブラウザ検証欄は対象6問の全文一致と明記した。`git diff --check` と新規ファイルの空白・改行・ローカルリンク確認も通過。

今回の構造・コピー文字列・画面確認は、出典の真偽全体や残る13問の採点妥当性を保証するものではない。
