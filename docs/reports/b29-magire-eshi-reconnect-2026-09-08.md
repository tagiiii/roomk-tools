# B-29 まぎれ絵師のゲスト保持・復帰

2026-09-08。基準HEAD `30c7e6eb0b3d3730ddccf82f0436b31ccdc1bb33`（B-28公開後）。P-11承認の段階展開。

## 最新状態: コミット記録・PR／マージ準備（2026-09-08）

オーナーの「マージまで」承認により、作業ブランチ `fix/magire-eshi-guest-reconnect-20260908` で実装3ファイルを `3fcc084ab3a8513c774bef56cd862b3506050314` にコミットした。本体SHA256 `3532d1b03ae67d198f02bb07161dcd3c0a294f29c00dae46f9d946538e431e3c` は検証済み版から不変。文書・証拠8ファイルを後続の文書コミットへ分離する。文書コミットのhashは自己参照を避けてgit logで確認する。

B-29は実装・隔離検証の完了としてチェック済み。実SDK44ケース・補助VM47群、同系統の別コンテキストによる最終独立レビューの範囲・限界は以下を継承し、実SDKの独立再実行や本番確認とは扱わない。この記録時点では未push・実PR未作成・未マージ・未公開で、承認済み公開工程へ進むための準備状態。以下の「未コミット」「完了チェック保留」等は各前工程当時の履歴として残す。

## 前工程の状態（履歴）

実装・隔離検証・最終独立レビュー済み。実SDK44ケース・補助VM47群が通過。利用制限で中断した追加1群・保存証拠・固有文書の独立レビューも再開して通過した。未コミット・未公開で、B-29の完了チェックは実装コミットを記録する工程まで付けない。

## 対象と維持する仕様

magire-eshi本体・固有AGENTS・updates先頭1件と検証記録を対象にする。共有コード・rules・設定・CI・他アプリ・180個のお題は変更しない。iPhone操作・本番Firebase接続は行わない。P-12など判断保留6件、A-11未判定44箇所は据え置く。

- waiting／reveal_role／drawing／discussion／voting／result／doneを通じ、guest本体と接続情報を分離する。同一保存sessionの復帰だけを対象とし、本人認証や別端末からの復帰は追加しない。
- 一時切断は在籍とし、手番を勝手に飛ばさず復帰を待つ。明示退出のときだけ、固定drawOrder上の既存スキップ、絵師退出警告・中断、在籍者による投票集計を維持する。
- 待機開始と次ラウンドは全ゲスト接続待ち。最小ゲスト3人、ホストは描かない・投票しない・絵師にならない仕様を維持する。
- 役割・確定済みストローク・確認済み・既投票を保持する。描きかけ／未送信線は切断時に破棄し、復帰保証の対象外とする。
- 600×450の論理座標、2px間引き・600点制限、固定描画順・2周、お題秘匿・ホスト折りたたみ、逆転当てと既存勝敗は変更しない。

## 実装設計

初回joinは一時value購読を保持し、古いsnapshot fallbackを使わない。presence登録は予約受付後にroom transactionで既存room／guest・役割・phase・TTLを確認し、予約・登録・取消を直列化する。退出／欠損／期限切れで世代を無効化する。

描画開始時のref／round／turn／接続世代を捕捉し、線の確定と手番更新をroom transactionで原子化する。pushキーは一度だけ生成し、再試行時に増やさない。旧非同期処理が新しい未送信線や操作状態を消さないよう所有tokenで保護する。

確認・投票・逆転回答とホスト側の手番スキップ／ラウンド遷移も最新存在・状態・roundを再確認する。逆転回答とB-22の未回答確定は先着を守り、判定後の遅い回答を拒否する。新しい勝敗値やラウンドIDは追加せず、既存roundを利用する。

TTL2分・done30秒は維持し、削除前の予約取消と捕捉ref／状態／round再検査を行う。旧版のplayer全体削除予約は別接続から取り消せないため、公開後は新版を読み込んだ新規ルームを利用する。

## 検証予定と制限

実SDK＋隔離Emulatorで7フェーズのリロード・連続断復帰、実ポインタ描画と確定、役割表示・確認・投票・逆転回答、次round、退出・TTL・30秒終了、1280px／375pxを確認する。人工遅延・競合順序は補助VMで別に確認し、実通信の保証と混同しない。実装と独立レビューは別コンテキストに分離する。同系統レビューを異系統二重承認とは表記しない。

## 着手前確認

main／origin/mainは基準HEADと一致し、作業ツリーcleanから開始した。lint全16項目とA-11回帰114fixture＋9 shellケースが通過。A-11は71ファイル・未判定44・raw-source ERROR0であり、安全認定ではない。content-auditもexit0で既知coverage警告7件を維持した。

新規スクラッチ `b29-sandbox.Ghdh62` に `demo-roomk-b29` のAuth9099／RTDB9000／Hosting5500／UI4000をloopback限定で準備した。statsはno-op、CSPと静的gateで接続先・SDK初期化を確認する。準備時点では本体コピー・Emulator／ブラウザ起動は未実施で、既存B-26〜B-28のスクラッチ証拠は保持する。

独立設計確認では、投票UIの300ms待機を含めクリック時点のref／round／接続世代を捕捉すること、strokeの旧finallyが新操作を壊さないこと、listenerをconfirmed／voteの正とすることを追加確認条件とした。通常の途中画面への常設退出は追加せず、新しい切断待ちoverlayと既存退出導線を使う。旧AGENTSの「席を外すとスキップ」は明示退出に限定して訂正する。

## 初回レビューの指摘

初回候補 `a6f5a72ccb02db743251549c25f348214e35925c06765e1f16e1962e982a0326` は構文・lintを通過したが、独立レビューで開始／次roundの自己失効を指摘した。transactionの未確定提案がvalue経由でstate.roundを進め、その後競合でcallbackが再試行されると、旧roundを捕捉した自分自身のガードでabortする順序である。実関数のメモリ対照で再現し、実SDKの再試行全体を再現したものではない。開始と次roundをapplyLocally:falseにする最小修正と回帰確認を行う。初回版のブラウザ結果を最終版合格として転用しない。

## 修正版の検証経過

本体修正版SHA256は `3532d1b03ae67d198f02bb07161dcd3c0a294f29c00dae46f9d946538e431e3c`。開始／次roundの2transactionだけをapplyLocally:falseへ変更し、逆置換で初回SHAに一致することを独立レビュアーが確認した。コード再レビューは追加修正要求なし。

初回版での実UI3ケースはscratchへ参考ログとして分離した。修正版に差し替えて4contextすべてをreloadし、新規ルームからhost＋guest3人の初回参加、役割確認、実ポインタ6筆による自然なdiscussion遷移、全員投票によるcaught、絵師の逆転回答とホストの正答判定を再実行して通過した。

補助VMは主担当と独立レビュアーが46群を再実行し通過した。これは合成transaction・接続予約・pointer順序であり実SDKの再試行プロトコルではない。Canvas／DOMは最小stubで、実レイアウトや秘密表示の検証はSDKブラウザ側に分ける。旧予約は古いpathへの直接removeで模擬し、cancel helperは呼出しspyである。

その後、実hostSkipReversalを別VM contextで呼んで同じroom参照へ確定し、元guestの遅い回答を拒否する1群を追加した。主担当と、再開した独立レビュアーがそれぞれ最終47群を再実行し[保存JSON](evidence/b29-magire-eshi-presence-2026-09-08.json)との完全一致を確認した。[再現スクリプト](evidence/b29-magire-eshi-presence-2026-09-08.mjs)を保存。本体SHAは不変。

## 実SDK最終結果と方法

[実行補助スクリプト](evidence/b29-magire-eshi-emulator-2026-09-08.mjs)と[結果JSON](evidence/b29-magire-eshi-emulator-2026-09-08.json)を保存した。実行補助スクリプトはCLIへ渡す段階実行コードを出力する形式で、単独のPlaywrightテスト一式ではない。各段階で専用4context・role・URL・demo project・Auth／RTDB・CSPを確認する。

担当者の利用制限でvotingまで21ケースを確認したところで中断したため、主担当がgate・本体SHA・保存レポートを確認して引き継いだ。result以降と追加境界を実行し最終44ケースが通過した。初回候補の3ケースは含めない。

| 確認範囲 | 結果と制限 |
|---|---|
| 実UIラウンド | 初回参加、役割確認、6筆の実pointer描画、自然な相談画面遷移、全員投票・逆転回答とホスト判定まで通過 |
| 7フェーズ | 各リロードと10秒Offline→Onlineを2回ずつ。接続ID以外のplayers/game/round/statusを比較。フェーズは保存済みgameを使うREST fixtureで設定し、自然遷移試験と区別 |
| 秘密表示 | reveal／drawing／discussionで全guestを再読込し、指定role-card／topic-chipにおける通常絵師のお題表示・まぎれ絵師のお題非表示を確認。DB認可や全DOMの秘密不存在を保証しない |
| 逆転 | UI回答を先に確定後、hostSkipReversalを直接呼んでも答えを上書きしない。UIスキップ先行も確認したが、その後の回答helperは入力フォーム消失でreturnする経路。SDKのキュー済み回答競合を再現したとは扱わずVMへ分離 |
| 描きかけ | 実pointerで作った未確定線がOffline時に破棄され、確定ボタンdisabled、復帰後も未確定線なし・保存game不変を確認 |
| 次round・勝敗 | UI次roundでgame・確認・投票を初期化しpresenceを保持。escaped／abortedは保存result fixtureでホスト操作表示を確認 |
| 退出・欠損 | drawing手番guestのleaveGame呼出しでplayer削除・線保持・次手番へ。fake退出後のホスト中断UIでaborted。最小人数、欠損playerと不正session4種、host退出を確認。退出helper呼出しは通常途中画面の退出ボタンクリックではない |
| done削除 | 実30秒タイマーで30,037ms。room削除、host/guestのTOP復帰、再切断・再読込後もroom再生成なし |
| TTL | host切断時刻125秒前／119秒前のfixtureで16ms／1,022msに削除。2分間の実時間待機ではない |
| 表示 | 1280px／375pxで横スクロール検査。主担当がdrawing切断待ち375px・drawing host1280px・result host1280pxの3枚を目視。result画像は上部表示の範囲で、全画像の目視承認ではない |

観測した通信1,014件はloopbackと許可したSDK／fontの7 origin内。console102件はすべて意図的Offlineに伴うエラー、収集したpageerrorと予期しないconsoleは0件。ログ収集は各step中だけで、初回遷移やstep間を含む全期間ではない。保存時にquery tokenを除去し、公開JSONはorigin別件数・エラー分類を記載。スクラッチにはqueryを除いたstep別ログも保持した。

## 終了状態と残作業

rooms／statsはいずれもnullを確認。専用b29ブラウザを閉じ、所有CLI PID34963へSIGTERMを送った。CLI／Java PID35003が不在で、4000／4400／4500／5500／9000／9099にLISTENがないことを確認した。scratchと旧試験の証拠は保持し、本番Firebase・iPhoneには触れていない。

最終lint16項目・diff check・VM47保存JSON一致を主担当が再確認した。お題TOPICSと隣接定数は基準版と完全一致。本体差分353追加89削除は200行目安を超えるが、presence・描画と手番の原子化・遅延操作の失効・退出・期限削除を片側だけ導入すると欠損や再生成が残るため、1アプリの不可分のライフサイクル変更としてまとめた。

最終独立レビューは再開してPASSとなった。VM47群の再実行・保存JSON一致、SDK44ケースの手順／保存証拠、固有AGENTS／updates・本報告を確認し、追加のコード修正要求はない。SDK公開JSONとスクラッチのサニタイズ済みログは、スキップ後の1ケースの名称を「入力フォーム消失後にhelperがreturnする」と限定した訂正以外は全内容一致。1,014件のorigin集計・console102件の分類・pageerror0・本体SHA・環境停止も独立確認した。SDKを独立に再実行したという意味ではない。実行先ガードはブラウザを使わないmockで正常1件・誤設定拒否7件も確認した。

残作業はコミットとPR作成。利用制限のリセットは購入・消費していない。B-29のチェックはコミット記録工程まで未完了のまま、マージ・公開は別工程とする。
