# P-10 kbn08 の修正・検証記録（2026-09-07）

## 結果と承認範囲

前工程で提示した `kbn08` の最小修正案に対し、オーナーが「次をお願いします」と回答したため、実装・検証・記録を実施した。コミット・公開への承認には拡張せず、**未コミット・未公開**のまま保持する。push・PR・マージ・デプロイ・本番 Firebase 接続・iPhone 操作なし。

`apps/quiz/questions.js` の `kbn08` 解説1箇所のみを変更した。

- 変更前: `迷いながら道を歩き`
- 変更後: `迷いながらろばに乗って進み`
- 問題文・選択肢・正答番号・難易度・ID・問題順は不変。他479問と全パックメタデータも完全一致。
- 根拠: [小学館・平凡社の辞典「推敲」](https://kotobank.jp/word/推敲-539689)の故事説明。前回の提案に加え、独立レビュアーが関連本文を再確認した。
- `apps/updates.json` は未変更。既存解説の周辺描写の訂正で、問題追加や遊び方の変更ではないため軽微bugfix扱い。

先行10問を含め、初回の具体的矛盾候補12件は **11問ローカル修正・kbn10の監査指摘撤回1件**となった。初回候補の処理が一巡したという意味であり、全320問の事実保証やP-10全体の完了ではない。[前回追加照合の未解消26問](p10-night-followup-2026-09-06.md)と、継続検証の範囲・頻度・未検証の扱いは引き続き残る。

## 差分の固定と独立レビュー

- HEAD: `3a8b174d07b0e5302aeaf406e75f16fd38d7966e`（変更なし）
- 変更前SHA-256: `058162a845442933af2e95caf7442dda5b5a3f4b3d66821a1107db57472b19ae`
- 変更後SHA-256: `7aab68b61b8ebdba4bddc724acd6423ec016f9c2108f15541fbc325d4b826c2b`
- [変更前後と検証証拠](evidence/p10-kbn08-fix-2026-09-07.json)
- [スナップショット検証器](evidence/p10-kbn08-verify-2026-09-07.mjs)

実装は別コンテキストへ委譲し、実装者とは別のコンテキストが読み取り専用でレビューして承認。主担当も独立に検証器を実行した。同系統の別コンテキストによる確認であり、異系統モデルの二重承認とは称さない。

指定の句1箇所をメモリ内で逆置換すると変更前SHAへ完全一致することを確認。全24パック480問の比較で今回の変更が `kbn08` だけであること、HEADからの累計変更が指定11問であることも検証した。HEADとの差分は累計25行置換で、先行修正を巻き戻していない。

前回追加照合の40行は現行問題とも本文一致する。旧台帳のSHA・判定・残余26件は履歴として変更しない。旧 `p10-night-verify-2026-09-06.mjs` は修正前SHA固定の検証器なので、今回の修正後にはSHA検査で停止するのが仕様。現在の確認には上記の新検証器を用いる。検査を通すために旧記録を書き換えない。

## ブラウザ・セキュリティ確認

Browserスキルの手順で、実ファイルを配信するローカルサーバーを通常UIから操作した。`stats.js` の応答のみ無処理とし、検証サーバーで `connect-src 'self'` を指定。問題データ・状態・正答判定はスタブ化していない。

- パック「ことばの文化」・「ぜんぶ」で開始。Q11として出た `kbn08` の①を選び、「せいかい！」と正答・修正後の解説を確認。回答後の選択肢3件は無効化。
- 1280px／375pxの両方で横スクロールなし。通常スクリーンショットで解説を確認。375pxでは選択肢幅347px・解説幅299px、解説末尾・次問・途中終了ボタンまで収まった。
- 次問 `kbn09` で意図的に誤答し「おしい！」と正答が表示されることを確認。全20問を終了して `19 / 20`、TOPへ戻れることを確認。既存問題の全出典を再検証したという意味ではない。
- タブのコンソールエラー0件。
- 実 `app.js` のコピー文字列生成関数をNode VMで検証。修正後の解説・正答・丸数字が一致。コピー先だけをスタブ化し、実クリップボード転送は未検証・ユーザーのクリップボードは操作していない。
- 追加JS・HTML・通信処理なし。解説は既存の `textContent` 経路で表示され、今回の訂正による新たなHTML解釈経路はない。全アプリの包括的セキュリティ監査ではない。

初回に既存の都道府県パック表示を古い内容かと疑い、8765から8767へ接続先を変更した。その後の原本照合で同パックはもともとのデータに含まれると確認し、キャッシュ不具合の推測を撤回した。最終動作確認は8767で実施し、表示されたkbn08の解説と現行原本が一致した。

## 機械チェックと後処理

- `node docs/reports/evidence/p10-kbn08-verify-2026-09-07.mjs`: PASS。
- `bash scripts/lint.sh`: 着手時と修正後に全16項目通過。
- `node scripts/content-audit.mjs`: exit 0。既知coverage警告7件を維持、アプリ内完全重複0件。
- `git diff --check`: exit 0。未追跡の新規成果物も別途、行末空白・末尾改行・ローカルリンクを確認。
- 検証タブを閉じ、画面幅指定を解除。今回起動したサーバーPID1647／3033を停止し、8765／8767の待受なしを確認。今回の一時 `server.mjs` と空の一時ディレクトリだけを除去した。問題ファイル・証拠・既存の作業は保持。

作業ツリーには11問の修正と今回までの文書・証拠が残り、cleanではない。アプリ側の変更は引き続き `apps/quiz/questions.js` のみ。次の工程は差分を確認した上でのコミット・PR準備だが、今回の承認で実施していない。マージ・公開も別途判断し、P-10／C3と他の保留項目は閉じない。
