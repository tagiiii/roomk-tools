# P-10 do08 の修正・検証記録（2026-09-07）

## 結果と承認範囲

オーナーが承認した「do08だけを単一正答に最小修正する作業」の範囲で、問題文・選択肢・解説だけを変更した。最終の熟語問題の文面は実装者が選定し、別コンテキストでレビューした。変更前の「掛ける／架ける／懸ける」の一般的な動詞の可否ではなく、橋をかけることを意味する熟語「かきょう」の表記を問う形に限定する。

- 問題: 橋をかけることを表す熟語「かきょう」。漢字で書くと？
- 選択肢: ① 掛橋 ／ ② 架橋 ／ ③ 懸橋
- 正答: ② 架橋（`answerIndex: 1`を維持）
- 解説: 「かきょう」は「架橋」と書き、橋をかけることや、その橋を表す。「架」は、かけ渡すという意味の漢字。

ID、難易度2、正答番号、選択肢数、問題順、全パックメタデータは固定。他479問も完全一致した。アプリの採点処理は変更していない。今回の承認はdo08の実装・検証・記録に限り、コミット・push・PR・マージ・デプロイへの承認には拡張しない。未コミット・未公開。本番Firebase接続・iPhone操作も行わない。

修正は[第2回追加照合](p10-followup2-2026-09-07.md)で見つかった採点上の候補を扱う別工程。同報告と旧台帳の未承認・未修正・22問という記載は、追加照合時点の履歴として保持する。

## 根拠と旧残余の扱い

[漢字ペディアの「架橋」](https://www.kanjipedia.jp/kotoba/0000667100)で熟語の読み・表記と、橋をかけること／その橋という意味を確認。[「架」の意味](https://www.kanjipedia.jp/kanji/0000666600)と、[小学館の辞典「架橋」](https://kotobank.jp/word/架橋-459928)でも照合した。新しい問いは「かきょう」という読みと意味を明示している。

旧問題の「懸けるは橋には合わない」という全面的制限は撤回した。その断定の正しさが新たに裏付けられたという意味ではない。また、「架ける」が誤りだったのでもなく、旧「掛ける」を一律誤答にした動詞問題を熟語問題へ変更することで一意採点の条件を整えた。

第2回追加照合の26問のうち、現行本文が変わったのはdo08だけで、他25行は原文一致のまま。旧未解消22問からdo08を現行の追跡対象から外し、残る21問をそのまま引き継ぐ。旧台帳の`outcome: unresolved`と`newFindings`は書き換えない。

| 分野 | 継承する未解消ID |
|---|---|
| 科学・暮らし（6） | z07、z08、s06、s08、ks08、ks13 |
| 漢字・文化（7） | kj19、kbn07、zk201、zk203、zk204、zk207、zk211 |
| 慣用句・語義（6） | ky01、ky04、ky18、ky20、ru18、ma07 |
| 言語（2） | sk03、sk05 |

21問は誤り21件でも全バックログの未完了総数でもない。初回320問・全480問の事実保証、P-10／C3の範囲・頻度・未検証リスクの運用判断は完了していない。先行11問の修正はPR #13で既に公開済みだが、今回のdo08修正は未公開。

## 差分の固定と独立レビュー

- 基準コミット（承認時のHEAD）: `96f3cf7d99fd9c19d09ba9d893b913752543dcbe`
- 変更前SHA-256: `7aab68b61b8ebdba4bddc724acd6423ec016f9c2108f15541fbc325d4b826c2b`
- 変更後SHA-256: `18d6abfe00f2348a1c182342c11992ec4a77d4d8a3fb40376724d84dc2fe5dc1`
- [変更前後と検証証拠](evidence/p10-do08-fix-2026-09-07.json)
- [新スナップショット検証器](evidence/p10-do08-verify-2026-09-07.mjs)

実装者とは別コンテキストの文化担当が最小修正をレビューし承認した。記録担当も現行本文と上記出典本文を照合した。同系統の別コンテキストによる確認であり、異系統モデルの二重監査とは称さない。

新検証器は基準コミットの原本と固定SHAで比較し、全24パック480問・変更1問・不変479問・変更3フィールド・位置とメタデータ固定を確認する。初回／夜間の旧8台帳は基準コミットとのバイト一致、第2回の4台帳と検証器は修正工程開始時のSHA一致で保持を確認する。

旧 `p10-followup2-verify-2026-09-07.mjs` は変更前SHA固定なので、do08修正後にSHA検査で停止するのが仕様。旧検証器や旧台帳を書き換えて通すことはしない。新検証器も将来の別の問題編集では意図的に停止する履歴スナップショットであり、常時通る汎用テストや出典の真偽証明ではない。

実`app.js`の`formatQuestionText`・`copyQuestion`・`copyAnswer`を抽出し、コピー先だけをスタブ化してNode VMで実行。新しい問題・選択肢・②架橋・解説が一致した。OSクリップボードへの実転送は未検証で、ユーザーのクリップボードは操作していない。

## ブラウザ・セキュリティ確認

主担当がBrowserスキルの手順で通常UIを操作し、記録担当が結果を受領した。実リポジトリの`apps`を`http://127.0.0.1:8767/quiz/`で配信し、`stats.js`だけを無処理の応答に差し替え、CSPで`connect-src 'self'`を指定。問題データ・採点処理はスタブ化していない。

- PC幅1280pxで`scrollWidth: 1280`、横スクロールなし。fullPage画像で問題・選択肢・解説・次へ・途中終了の配置を確認。
- do08の②を選んで「せいかい！」・選択肢の`correct`表示を確認。
- ①と③をそれぞれ選んだ場合は「おしい！」・選択肢の`wrong`表示と②の`correct`表示を確認。回答後は3ボタンとも無効になり、正答②と新解説が表示された。
- 通常UIで20問を終了して`7 / 20`、再プレイ、途中終了して`1 / 7`を確認。他の問題は任意回答であり、全問の事実関係を再監査したという意味ではない。
- 検証タブのwarning／errorログは0件。
- **375px幅、TOP復帰、iPhone操作、OSクリップボードへの実転送は今回未実施。** 過去の別問題の検証を今回のPASSとして流用しない。

変更は問題データの文字列だけで、採点処理・HTML・通信コードの追加はない。包括的なセキュリティ監査を実施したという意味ではなく、本番Firebaseには接続していない。

## 機械チェックと後処理

- `node docs/reports/evidence/p10-do08-verify-2026-09-07.mjs`: 記録担当と主担当でPASS。
- `bash scripts/lint.sh`: 主担当の実行で全16項目通過。
- `node scripts/content-audit.mjs`: 主担当の実行で通過。既知coverage警告7件を維持し、アプリ内完全重複0件。crossVariant 2／structural 12／similar 268は検出分類の件数で、新規不具合数ではない。
- `git diff --check`: 通過。今回新規3成果物も別途JSON解析・行末空白・末尾改行・ローカルリンクを確認し、報告表の21IDが証拠JSONの継承集合と一致することを確認した。

主担当が検証タブを閉じてタブ一覧が空であることを確認し、検証サーバーsession57276をCtrl-Cで停止した。今回の自作`server.mjs`と空の`/tmp/roomk-do08-check.1oxbWd`のみを除去し、8767に待受がないことを確認した。問題ファイル・証拠・既存の作業は保持。

既存の第2回追加照合の未コミット差分は保持する。今回の記録追加は新規報告・証拠・検証器とREADME索引／P-10経過の追記のみで、コミット・公開は行わない。
