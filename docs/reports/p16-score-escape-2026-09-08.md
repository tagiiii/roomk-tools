# P-16 点数表示のエスケープ修正

2026-09-08。オーナーの「マージまでお願いします」により、[隔離検証](a11-score-sandbox-2026-09-08.md)で提案した4補間の修正・検証・PR・マージを承認。基準は `8714c57927a01e50d5b1130d865ad796484b9ada`。

## 変更とコミット

| 対象 | 変更 | コミット |
|---|---|---|
| do-mannaka index.html:1526/1558 | score-list・final-rankingの `${score}` を `${escapeHtml(score)}` へ | `85c78ed` |
| tatoe-gp index.html:1160/1877 | 同上 | `5e1c0b2` |

各アプリ2行の補間変更のみ。既存の共有escapeHtmlを利用し、点数計算・順位・保存・通信・rules・shared・CSS・画面遷移は変更しない。軽微な修正のためupdates.jsonは追記しない。

## 固定入力による回帰確認

[検証器](evidence/p16-score-regression-2026-09-08.mjs)と[結果JSON](evidence/p16-score-regression-2026-09-08.json)を保存。基準ソースのSHAと、各アプリ2置換以外の全バイト一致をassertし、実関数4本と実escapeHtmlを抽出してVM上で実行した。DOMはプロパティ捕捉stub、SDK・ネットワーク・storageなし。

- 数値12・文字列12・null・0・無害なHTML点数、空players、HTML名前、通常4人の順位を各4箇所で確認し、32ケースPASS。
- 点数の太字タグは4箇所ともエスケープされる。優勝者名・点数のtextContentは従来どおりの文字列代入。
- 通常4人の点数・順位・名前・優勝者を修正前関数の捕捉結果と完全一致で確認。
- 修正前の[検証器](evidence/a11-score-sandbox-2026-09-08.mjs)には履歴再現用 `--baseline` を追加。固定Gitソースで旧28ケースと保存JSONを再現できる。既存JSONは不変。引数なしは従来どおり作業ソースの一致を要求するため、修正後は意図的に失敗する。

## ブラウザ確認と限界

Browserスキルを使用し、実関数の捕捉HTMLをスクリプトなしの一時ページで解析した。4箇所とも `b[data-a11-score-probe]` は0個、点数のtextContentはタグを含む元の文字列＋ptだった。別の新規タブでも同じ表示を確認し、errorログは空。

共有CSSと各アプリの既存CSSを含む375px／1280px幅のiframeで、4箇所のHTML点数と通常12ptの計16表示をDOM snapshotで確認した。通常12ptの入力は捕捉HTML内の点数字句だけを差し替えた表示用対照であり、VMの数値テストとは別。外部フォント・Firebase SDK・アプリ全体はロードしていない。これは点数カードの限定確認で、実端末・全画面・操作・同期の検証ではない。

iframe内部寸法の取得はブラウザ操作層の制約で失敗し、同じタブにMutationObserverのエラーが記録された。横スクロール量の測定やスクリーンショットによる目視確認は完了していない。上記4箇所のDOM構造確認はiframeを使わないページへ分離し、新規タブで再確認した。スクリプトなしの隔離ページの結果を、実アプリのコンソール無エラー保証に置き換えない。

本番Firebase・保存セッション・iPhone・クリップボードは操作しない。書き込み権限や他ユーザーへの攻撃可否、JavaScript実行は未検証。不正な数値による順位の乱れも修正対象外。

## レビュー・機械チェック・残余

実装者と別コンテキストの同系統エージェントが、4行差分、共有ヘルパー、基準照合、32ケース・旧28ケースと再生成JSONの完全一致を独立確認し承認。主担当も差分・lint・ブラウザのDOM構造を確認した。別系統モデルによる二重レビューとは呼ばない。

lint全16項目とdiff checkはPASS。検査器自体は無変更で、裸参照の未判定は48→44（該当4補間が裸参照でなくなったため）。残る44の安全認定やA-11全体完了を意味しない。保留7件は不変。

検証タブ・一時サーバーを終了し、一時HTML2件と空スクラッチディレクトリを削除した。元の捕捉HTMLは恒久JSONから再構成できる。先行の棚卸し・隔離検証文書も履歴として同じPRへ含める。旧報告の未承認・未コミット・未公開表記はその工程時点の記録。

本記録はPR作成前のもの。マージ結果とPagesの実行結果はGitHubのPR・Actions記録と最終作業報告で確認する。
