# P-10 科学3問の承認修正・検証記録（2026-09-07）

## 承認範囲と基準

オーナーが [科学3問の修正案](p10-science-three-proposal-2026-09-07.md) の z07／s06／s08 の全文案を承認したため、当該3問だけを実装・検証した。文案と機関本文4ページの出典・取得範囲は同提案書が正本であり、旧提案書の「未承認・未実装」は当時の履歴として変更しない。他の残余問、P-10／C3の検証運用や未検証リスク受容へ承認を拡張しない。

- 公開済み基準: main `6d818eb3e9bd5dc4b3a455088406ee6785367c29`（PR #15 マージ後）。
- 修正前 `apps/quiz/questions.js` SHA-256: `ea1cfedccfcc22a577379f7d06b09f73c8c9e92686f8a2a3f7ab8adf68f993e4`。
- 修正後 `apps/quiz/questions.js` SHA-256: `e11de150d284b666c98e53369c102ecb3069816af3a60569ef46c2512b6032d3`。
- 提案書SHA-256: `58c75ae622b65559685c51a83565584dada402d53ac27ce87d5ab0964abb0339`（変更なし）。

| ID／パック | 承認された変更フィールド | 変更の意図 |
|---|---|---|
| z07／zatsugaku-3choice | question、explanation | フェネックの暑さ対策を問う形へ絞り、放熱と聴覚の機能順位を外す。 |
| s06／series-2choice | question、choices、explanation | 若いひまわりが夜に東向きへ戻る方向を問う形へ変更し、昼夜の速度比較を外す。正答②は新しい「東」の選択肢となる。 |
| s08／series-2choice | question、explanation | ホッキョクグマの外側の長い毛に限定し、下毛への一般化と見かけの光学説明を外す。 |

ID・difficulty・answerIndex・配列位置・パック情報と他477問は不変。旧主張がすべて誤りと証明されたのではなく、未裏付けの比較や一般化を今回の出題から除く変更である。

## 機械検証・独立レビュー（担当結果受領）

実装担当と主担当が新しい [科学3問の固定検証器](evidence/p10-science-three-fix-2026-09-07.mjs) を実行し、PASSを確認した。

- 固定mainと提案書SHAを照合。24パック・480問を保持し、指定3問の7フィールドだけを変更。ソース全体も厳密な文字列7置換に一致し、文案全文・他477問・全メタデータ・正答番号②は不変。
- 旧証拠・検証器28ファイルはbyte不変。旧スナップショット検証器と提案書は歴史固定のまま保持した。
- 変更のないapp.jsの実コピー組立関数を用い、転送先だけをスタブ化して3問の計6出力文字列を全文照合。OSクリップボードへの実転送は未実施。
- lintは編集前後とも全16項目PASS。content-auditはexit 0（3405項目・完全一致255群・同一アプリ完全重複0・別variant2・構造12・類似268、既知coverage警告7件継続）。diff checkはexit 0。

同系統の別コンテキストによる独立レビューはsource・検証器ともPASS。既存のtextContent表示と選択肢のエスケープ経路は不変で、新しいXSS経路や実行コードの追加はない。実ファイルを変えない模擬改変4件でも、検証器が期待するAssertionErrorを返すことを確認した。画面確認は下記の主担当報告を引用するもので、独立レビュー担当が再操作したとは扱わない。これらは固定差分・構造・生成文字列の検証であり、全事実や全意味重複の保証ではない。

## 主担当による画面確認（結果受領）

既存のブラウザ接続を用い、新規タブと新しいローカルorigin `http://127.0.0.1:8774/quiz` で実appsを配信した。オフラインのquizを対象とし、statsはlocalhostでno-op。本番Firebase接続は行っていない。

- PC 1280×720: 対象2パック各20問を全正答で完走し、それぞれ20/20。
- 375×812: 同じ2パック各20問を全誤答で完走し、それぞれ0/20。
- 各幅で重複しない40 IDを確認。各問のDOMを期待ソースに照合し、設問・全選択肢・解説全文、正誤フィードバック、回答後の全ボタン無効化・解説の非表示解除、横はみ出しなしを確認した。各完走後のTOP復帰も確認。
- 対象3問はPCで正答②。375pxではz07の誤答③、s06／s08の誤答①を確認。さらに375pxの雑学パックを再プレイし、15問正答後にz07の①を選択して誤答、途中終了15/16→TOPを確認。これで対象3問の全選択肢を検証した。
- PCの連続2択パックでも再プレイ→1問誤答→途中終了0/1→TOPを確認した。

対象3問×2幅の通常スクリーンショット6枚を目視し、表示は正常。フルページ撮影や撮影時だけのviewport拡大はしていない。PCのz07解説は画面下端にあるため、画像だけで全文可視とはせず、DOM全文一致の確認と区別する。

専用ログ `dev.logs` をerror／warn・limit 100で、初期表示、PC各パック完走、375px各パック完走、最後に取得し、すべて空配列だった。この試験経路・取得範囲の結果であり、全利用環境でのエラー不存在は保証しない。iPhone実機操作とOSクリップボードへの実転送は未実施。

後片付けではviewportのreset呼び出しが成功した。ただし直後のDOM値は375×812のままで、1280pxへ復元したという実測確認ではない。新規タブを閉じ、タブ一覧が空であることを確認。ローカルサーバーPID 66194のcommandを照合してSIGINTで停止し、8774のlistenerがないことを確認した。

## 残余と公開状態

承認された3問の実装検証を完了し、旧残余12問から z07／s06／s08 を対応済みとして除外した。次の9問を継承する：z08、ks08、ks13、kj19、kbn07、zk201、zk203、zk211、ru18。この残余は誤り数でもバックログ全体の未完了数でもなく、P-10／C3の運用判断は未決のまま。

今回の3問修正は未コミット・未push・未公開。今回工程の変更はquestions.jsの指定3問、新検証器、本報告、README索引1行、P-10最新履歴1行に限定し、既存の文書差分・旧提案書・旧履歴を保持した。公開済みmainの状態とは区別し、コミット・push・PR・デプロイは行っていない。
